// import type { Data, Filter, Sorting, WithIdentifier } from "../types"
// import { applyFilters } from "./apply-filters"
// import { applySorting } from "./apply-sorting"

// export const getCount = <T extends WithIdentifier>(data: Data<T>, filters: Filter[], sorting: Sorting[]) => {
//     console.log('getCount()')
//     return applySorting(applyFilters(data, filters), sorting).length
// }